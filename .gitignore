
# hv-managed (added by hv deck provision — idempotent)
.claude/
*.md
!README.md

# reviewed execution plans are versioned (exception to the deck-wide *.md rule)
!docs/plans/**

# build + deps
node_modules/
studio/node_modules/
arcade/node_modules/
go/bin/
wezterm-bridge/bin/
vendor/wezterm/

# bundled renderer output (esbuild)
studio/dist/
arcade/dist/

# packaged output
dist/

# secrets / local env — never commit (repo is public)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519
.npmrc
.netrc
.pgpass
